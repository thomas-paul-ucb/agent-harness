# agent-harness
A control layer for Claude tool-use agents — manages the decide→act→observe loop with retry/backoff on tool failures, stop conditions to prevent runaway loops, and structured output validation.
